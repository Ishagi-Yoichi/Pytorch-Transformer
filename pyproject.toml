[project]
name = "pytorch-transformer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "datasets>=4.8.5",
    "nn>=0.1.1",
    "tokenizers>=0.23.1",
    "torch>=2.11.0",
]
